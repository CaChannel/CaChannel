package:
  name: cachannel
  version: !!str 2.4.0

source:
  # build from PyPI source tarball
  #fn: CaChannel-2.4.0.tar.gz
  #url: https://pypi.python.org/packages/source/C/CaChannel/CaChannel-2.4.0.tar.gz
  
  # build from source tree (for conda-build > 1.9)
  #path: ..
  
  # build from mercurial repository
  hg_url: http://bitbucket.org/xwang/cachannel
  hg_tag: 2.4.0

  patches:
   # List any patch files here
   # - fix.patch
# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cachannel = cachannel:main
    #
    # Would create an entry point called cachannel that calls cachannel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - CaChannel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  requires:
    # Put any additional test requirements here.  For example
    # - nose
    - setuptools

about:
  home: https://pypi.python.org/pypi/CaChannel
  license: BSD License
  summary: 'CaChannel Interface to EPICS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
